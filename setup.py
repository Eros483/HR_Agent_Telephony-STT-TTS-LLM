# ----- Project Setup @ setup.py -----
# Sets up folders as packages

from setuptools import setup, find_packages

setup(
    name="HR-Agent",
    version="0.1",
    author="Arnab",
    packages=find_packages(),
)